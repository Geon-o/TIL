# schema use

### 📌 CREATE DATABASE (이름);
- 명령 프롬프트에 입력하게 되면 데이터베이스를 만들 수 있다.
    - ❗ 만약 삭제할 경우 DROP DATABASE (이름); 을 입력하면 삭제된다.
<br/>

### 📌 SHOW DATABASE;
-다음과 같이 입력하면 생성한 데이터베이스를 확인 할 수 있다.

<br/>


### 📌 USE (DB이름);
-다음과 같이 입력하게되면 생성된 데이터베이스를 사용할 수 있게된다.
