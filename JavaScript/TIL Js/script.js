//객체 생성
const player = {
    name: "G",
    point: 100,
    TrueAndFalse: true,
};
console.log(player);
console.log(player.name);

player.other = "Simple"; // 속성 추가
player.point = 50;
console.log(player)
